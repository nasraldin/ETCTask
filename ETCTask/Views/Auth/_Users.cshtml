@model IEnumerable<ETCTask.Models.Auth.User>

<div class="col-md-12">
    <p>
        @Html.ActionLink("Add User", "AddUser", null, new {data_modal = "", @class = "btn btn-primary"})
    </p>
    <table class="table table-bordered">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr class="item-user">
                <td>
                    @Html.DisplayFor(modelItem => item.UserName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new {id = item.Id}, new {data_modal = "", @class = "btn btn-success"})
                    @Html.ActionLink("Details", "Details", new {id = item.Id}, new {data_modal = "", @class = "btn btn-default"})
                    @Html.ActionLink("Delete", "Delete", new {id = item.Id}, new {data_modal = "", @class = "btn btn-danger"})
                </td>
            </tr>
        }

    </table>
</div>